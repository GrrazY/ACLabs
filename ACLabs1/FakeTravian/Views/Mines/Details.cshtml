@model FakeTravian.Models.Mine

@{
    ViewBag.Title = "Details";
    this.Layout = null;
}

<h2>Details</h2>

@if (this.Model.Type == FakeTravian.Models.ResourceType.Wheat)
{ 
    <h3>Wheat</h3>
    <p>Your population`s food is produced here. By increasing the farm`s level, you increase its crop production.</p>
}
else if (this.Model.Type == FakeTravian.Models.ResourceType.Clay)
{
    <h3>Clay</h3>
    <p>Clay is produced here. By increasing its level, you increase the clay production.</p>
}
else if (this.Model.Type == FakeTravian.Models.ResourceType.Iron)
{
    <h3>Iron</h3>
    <p>Here miners produce the precious resource of iron. By increasing the mine`s level, you increase the iron production.</p>
}
else if (this.Model.Type == FakeTravian.Models.ResourceType.Wood)
{
    <h3>Wood</h3>
    <p>The woodcutter cuts down trees in order to produce lumber. The further you extend the woodcutter, the more lumber is produced.</p>
}

<img src="~/Content/img/mine-@(this.Model.Type).png" />
<p>Current production: @(this.Model.Level * 13)</p>
<p>Production for next level: @((this.Model.Level + 1) * 13)</p>

@using (Html.BeginForm("Upgrade", "Mines"))
{
    //<input name="mineId" value="10" type="hidden"/>
    @Html.HiddenFor(m => m.MineId)
    <div>Do a fast upgrade ?: @Html.CheckBox("fastUpgrade") </div>
    <button type="submit">Upgrade</button>
}

